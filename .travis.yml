language: python
python:
  - "3.6"
# command to install dependencies
install:
  - pip install -r requirements.txt
  - pip install -U git+https://github.com/Rapptz/discord.py@rewrite
  # command to run tests
script:
  # Formatting compliance test
  - pycodestyle --show-source --show-pep8 --ignore=E121,E123,E126,E133,E226,E241,E242,E704,W503,W504,E402 --max-line-length=300 karma/* commands/* apollo.py models.py